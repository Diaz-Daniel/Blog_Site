{{#each blogs}}
  <div class="row mb-4 project">
    <div class="col-md-5">
      <h2>

        <a href="/blog/{{id}}">{{title}}</a>
      </h2>
      <p>Created by {{User.name}} on </p>
    </div>
    <div class="col-md-7">
      <p>
        {{body}}
      </p>
    </div>
  </div>
{{/each}}